# 10 安全（后续版本）

你当前的目标是 **MVP 先跑通**，因此本章仅保留占位与后续方向，不作为 MVP 的实现约束。

## 后续可以做什么（非 MVP）

- 文件路径穿越防护（严格 normalize + workspace root 检查）
- URL 白名单 / 域名白名单
- 网络策略（禁止内网、限制端口）
- 资源限制与审计（CPU/memory/IO/网络）
- 操作审计日志与回放

